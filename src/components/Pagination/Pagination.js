import React, { useState } from "react";
import styled from "styled-components";

const Slider = (props) => {
  return (
    <div>
      <div>Pagination</div>
    </div>
  );
};

export default Slider;
